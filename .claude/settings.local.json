{
  "permissions": {
    "allow": [
      "Bash(mkdir -p pages/api/products)",
      "Bash(mkdir -p styles)",
      "Bash(mkdir -p public)",
      "Bash(npm install:*)",
      "Bash(npx prisma:*)"
    ]
  }
}
