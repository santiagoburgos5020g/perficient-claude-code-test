The `design-patterns-reference` skill is for **subagents only**. Do NOT invoke
or apply this skill in the main conversation. It is loaded into subagents via
their `skills` frontmatter field.

It is applied exclusively within the Next.js/React frontend context
(`nextjs-react-best-practices`). It is NOT used for backend-specific work.
