The `agent-senior-nextjs-developer` handles **frontend code only** (pages, components,
hooks, styles, types in `pages/` and `features/`).

Backend work (API routes in `pages/api/`, Prisma operations, server-side services)
does NOT go through this agent.
